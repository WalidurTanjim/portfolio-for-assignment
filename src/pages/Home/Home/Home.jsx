import React from 'react';
import Hero from '../Hero/Hero';

const Home = () => {
    return (
        <section className='home'>
            <Hero />
        </section>
    );
};

export default Home;